services:
    - type: web
    name: oraculo-diana-de-vil
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
            - key: GROQ_API_KEY
        sync: false
            - key: MODELO
        value: llama-3.1-8b-instant